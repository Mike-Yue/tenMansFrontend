# Backend API base URL. VITE_ vars are embedded into the build and are public.
# Empty (or unset) makes the app use relative /api paths via the Vite dev proxy.
VITE_API_BASE_URL=https://tenmansbackend.onrender.com
